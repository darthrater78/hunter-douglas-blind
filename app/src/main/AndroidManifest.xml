<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- BLE scanning/connecting (API 31+). neverForLocation is valid because
         we filter on manufacturer data, not beacons — avoids the runtime
         location prompt on Android 12+. See docs/PROTOCOL.md / spec §2.1. -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="s" />

    <!-- Pre-31 equivalents. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <!-- Deliberately NOT declared yet, because nothing requests or uses them:
         - FOREGROUND_SERVICE / FOREGROUND_SERVICE_CONNECTED_DEVICE
           (build order step 9, spec §3.3) — add together with the <service>
           element itself. FOREGROUND_SERVICE_CONNECTED_DEVICE without a
           matching declared service is also a Play review failure.
         - POST_NOTIFICATIONS (build order step 10, spec §2.5) — add with the
           battery-sweep notifications.
         Declaring a permission before the feature that needs it costs the user
         a scarier install prompt and buys nothing. -->

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- TODO: replace this system placeholder with a real launcher icon (mipmap set). -->
    <application
        android:name=".PowerViewApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.PowerView">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PowerView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>

package com.scrivtech.powerview.widget

/**
 * TODO(build order step 9): the `android:configure` activity for
 * [ShadeActionWidgetPlaceholder] — pick which saved
 * [com.scrivtech.powerview.data.ShadeAction] (or, for the grid layout, up to
 * six) a given widget instance points at, and call
 * `setResult(RESULT_OK, intent)` with the app widget id per the standard
 * Android widget-configuration contract. See spec §3.2.
 *
 * See [ShadeActionWidget] for why this is deferred rather than stubbed with
 * unverified Compose/Glance code.
 */
public object WidgetConfigActivityPlaceholder
